apiVersion: v1
kind: ConfigMap
metadata:
  name: accord-web-assets-config
  namespace: accord
data:
  config.json: |
    {
     "production": false,
     "api": "https://{{ .Values.envUrl }}.{{ .Values.appDomain }}/accord/api/"
    }  
